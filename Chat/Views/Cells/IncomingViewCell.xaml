<?xml version="1.0" encoding="UTF-8"?>
<ViewCell
    IsEnabled="true"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Chat.Views.Cells.IncomingViewCell">
    <Frame
        FlowDirection="LeftToRight"
        Rotation="180"
        CornerRadius="5"
        BackgroundColor="LightBlue"
        HasShadow="False"
        Padding="10"        
        Margin="80,10,10,10">
        <StackLayout>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition
                        Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition
                        Height="auto" />
                    <RowDefinition
                        Height="auto" />
                    <RowDefinition
                        Height="auto" />
                </Grid.RowDefinitions>
                <Label
                    Grid.Row="0"
                    Grid.Column="0"
                    Text="{Binding User}"
                    FontAttributes="Bold" />
                <Label
                    Grid.Row="1"
                    Grid.Column="0"
                    Text="{Binding Text}"
>
                    <Label.Triggers>
                        <DataTrigger
                            Binding="{Binding IsExpired}"
                            Value="True"
                            TargetType="Label">
                            <Setter
                                Property="Text"
                                Value="Message Deleted." />
                       
                        </DataTrigger>
                    </Label.Triggers>
                </Label>
                <Label
                    Grid.Row="2"
                    Grid.Column="0"
                    HorizontalTextAlignment="End"
                    Text="{Binding SubmittedDate, StringFormat='{}{0:h:mm tt}' }" />
            </Grid>
             <Label
                Text="{Binding RemainingTimeString}" IsVisible="false">
                <Label.Triggers>
                    
                      <DataTrigger
                            Binding="{Binding HaveTimeout}"
                            Value="True"
                            TargetType="Label">
                            <Setter
                                Property="IsVisible"
                                Value="True" />
                        </DataTrigger>
                </Label.Triggers>
                </Label>
        </StackLayout>
    </Frame>
</ViewCell>