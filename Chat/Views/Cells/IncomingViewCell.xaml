<?xml version="1.0" encoding="UTF-8"?>
<cells:BaseMessageViewCell
    xmlns:cells="clr-namespace:Chat.Views.Cells"
    IsEnabled="true"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:partials="clr-namespace:Chat.Views.Partials"
    x:Class="Chat.Views.Cells.IncomingViewCell"
    Appearing="BaseMessageViewCell_Appearing"
    xmlns:converter="clr-namespace:Chat.Converter"
    
    >

    <!--<Frame
        FlowDirection="LeftToRight"        
        CornerRadius="5"
        BackgroundColor="White"
        HasShadow="False"
        Padding="10"        
        Margin="10,10,80,10"
          
        >-->

    <Frame
        FlowDirection="LeftToRight"        
        CornerRadius="5"
        BackgroundColor="White"
        HasShadow="False"                    
        VerticalOptions="StartAndExpand"
        HorizontalOptions="StartAndExpand"
        Margin="10"
        Padding="5"
        >


        <Frame.Resources>
            <converter:IsNullConverter x:Key="IsNullConverter"/>
        </Frame.Resources>
        
            <Grid >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition
                        Width="auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition
                        Height="auto" />
                    <RowDefinition
                        Height="auto" />
                    <RowDefinition
                        Height="auto" />
                </Grid.RowDefinitions>
                <Label
                    Grid.Row="0"
                    Grid.Column="0"
                    Text="{Binding User}"
                    FontAttributes="Bold" />

                <partials:MessageBody Grid.Row="1" Grid.Column="0" VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand"/>


                <Grid Grid.Row="2" >
                   <Grid.ColumnDefinitions>
                    
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="auto"/>
                    
                </Grid.ColumnDefinitions>
                
                    <Label
                        Text="{Binding RemainingTimeString}"
                        IsVisible="false"
                    HorizontalOptions="Start"
                    
                    FlexLayout.AlignSelf="Start"
                        Grid.Column="0"
                        >
                        <Label.Triggers>
                            <DataTrigger
                                Binding="{Binding RemainingTimeString,Converter={StaticResource IsNullConverter}}"
                                Value="false"
                                TargetType="Label">
                                <Setter
                                    Property="IsVisible"
                                    Value="true" />
                            </DataTrigger>
                        </Label.Triggers>
                    </Label>

                    <Label                    
                    Grid.Column="2"          
                    HorizontalOptions="End"
                    FlexLayout.AlignSelf="End"                               
                    Text="{Binding SubmittedDate, StringFormat='{}{0:h:mm tt}'}" 
                    TextColor="#abb7c0"
                    >

                    </Label>

                </Grid>


            </Grid>
        
    </Frame>
</cells:BaseMessageViewCell>