<?xml version="1.0" encoding="UTF-8"?>
<cells:BaseMessageViewCell
    xmlns:cells="clr-namespace:Chat.Views.Cells"
    IsEnabled="true"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:partials="clr-namespace:Chat.Views.Partials"
    x:Class="Chat.Views.Cells.IncomingViewCell"
    Appearing="BaseMessageViewCell_Appearing">
    <Frame
        FlowDirection="LeftToRight"        
        CornerRadius="5"
        BackgroundColor="LightBlue"
        HasShadow="False"
        Padding="10"        
        Margin="10,10,80,10"
          
        >
        <StackLayout>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition
                        Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition
                        Height="auto" />
                    <RowDefinition
                        Height="auto" />

                </Grid.RowDefinitions>
                <Label
                    Grid.Row="0"
                    Grid.Column="0"
                    Text="{Binding User}"
                    FontAttributes="Bold" />
                <partials:MessageBody Grid.Row="1" Grid.Column="0"/>
            </Grid>
        </StackLayout>
    </Frame>
</cells:BaseMessageViewCell>